// 删除元素
pop() 删除一个数组末尾的元素
shift() 删除一个数组首位的元素

// 添加元素
push() 数组末尾添加一个元素
unshift() 数组首位添加一个元素

splice() 任意位置添加或删除元素

concat() 连接2个或更多的数组
every() 对数组中的每一项运行给定函数，如果函数对每一项都返回true，则返回true
filter() 对数组中的每一项运行给定函数，返回函数会返回所有返回true的项组成的数组

